{
  "buildCommand": "NODE_ENV=development pnpm install --frozen-lockfile && pnpm --filter @scc/types build && pnpm --filter @scc/utils build && pnpm --filter @scc/ui build && pnpm --filter @scc/auth build && pnpm --filter @scc/web build",
  "framework": "nextjs",
  "installCommand": "echo 'Installed via buildCommand'",
  "build": {
    "env": {
      "NEXT_PUBLIC_API_URL": "https://scc380-api-prod.loca.lt"
    }
  }
}
